name: (stub) RC build
on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+rc[0-9]+"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Would build RC image for ${{ github.ref_name }}"
